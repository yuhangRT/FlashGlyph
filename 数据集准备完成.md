# ✅ AnyWord-3M 数据集下载完成！

**日期**: 2026-01-05
**状态**: ✅ 成功完成
**位置**: `./dataset/`

---

## 数据集概览

### 总大小: 200 GB

| 数据集 | 大小 | 内容 |
|--------|------|------|
| **LAION** | 118 GB | 5个分片 (laion_p1-p5.zip) + JSON |
| **Wukong** | 75 GB | 5个分片 (wukong_1of5 到 5of5) |
| **OCR数据** | 7.3 GB | 7个OCR数据集 (Art, COCO, LSVT等) |
| **JSON配置** | 1.6 GB | anytext2_json_files.zip |

### 详细目录结构

```
./dataset/
├── anytext2_json_files.zip     (1.6 GB)  ✅ 已复制
├── README.md                              ✅ 已复制
├── ocr_data/                    (7.3 GB)  ✅ 已复制
│   ├── Art/
│   ├── COCO_Text/
│   ├── LSVT/
│   ├── MTWI2018/
│   ├── ReCTS/
│   ├── icdar2017rctw/
│   └── mlt2019/
├── laion/                       (118 GB)  ✅ 已复制
│   ├── data_v1.1.json           (6.0 GB)
│   ├── laion_p1.zip             (23 GB)
│   ├── laion_p2.zip             (23 GB)
│   ├── laion_p3.zip             (23 GB)
│   ├── laion_p4.zip             (23 GB)
│   └── laion_p5.zip             (22 GB)
├── wukong_1of5/                 (15 GB)   ✅ 已复制
├── wukong_2of5/                 (16 GB)   ✅ 已复制
├── wukong_3of5/                 (15 GB)   ✅ 已复制
├── wukong_4of5/                 (16 GB)   ✅ 已复制
└── wukong_5of5/                 (13 GB)   ✅ 已复制
```

---

## 验证数据集完整性

```bash
# 检查总大小
du -sh ./dataset
# 输出: 200G

# 检查 LAION 文件
ls -lh ./dataset/laion/
# 应该看到 5 个 zip 文件，每个约 22-23 GB

# 检查 OCR 数据
du -sh ./dataset/ocr_data/*
# 应该看到各个 OCR 数据集子目录

# 验证 ZIP 文件完整性
file ./dataset/laion/laion_p1.zip
# 输出: Zip archive data
```

---

## 下一步操作

### 选项1: 开始训练 (需要解压数据)

```bash
# 1. 解压 JSON 配置文件
cd ./dataset
unzip anytext2_json_files.zip

# 2. 解压需要的 OCR 数据 (示例)
cd ocr_data/Art
unzip Art.zip

# 3. 解压 LAION 数据 (如果训练需要)
cd ../../laion
unzip laion_p1.zip  # 根据需要解压

# 4. 修改 train.py 中的数据集路径
# 将数据集路径指向 ./dataset/

# 5. 开始训练
cd /home/zyh/AnyText2
conda activate anytext2
python train.py
```

### 选项2: 评估模型 (使用原始数据)

```bash
# 评估脚本可能可以直接使用 zip 文件
cd /home/zyh/AnyText2

# 运行 OCR 准确率评估
bash eval/eval_ocr.sh

# 运行 CLIP 分数评估
bash eval/eval_clip.sh
```

### 选项3: 释放磁盘空间

如果确认数据集完整且不再需要原始缓存：

```bash
# 删除 ModelScope 缓存 (节省 200 GB)
rm -rf /home/zyh/.cache/modelscope/hub/datasets/iic/AnyWord-3M

# 验证项目副本仍然可用
du -sh ./dataset
# 应该仍然是 200G
```

---

## 训练配置

### 更新 train.py 中的数据集路径

编辑 `/home/zyh/AnyText2/train.py`，找到数据集路径配置（约第 71-101 行）：

```python
# 原始路径可能类似这样:
# data_root = '/path/to/AnyWord-3M'

# 修改为:
data_root = '/home/zyh/AnyText2/dataset'

# 或者分别指定各部分路径:
data_paths = {
    'wukong': [
        '/home/zyh/AnyText2/dataset/wukong_1of5',
        '/home/zyh/AnyText2/dataset/wukong_2of5',
        '/home/zyh/AnyText2/dataset/wukong_3of5',
        '/home/zyh/AnyText2/dataset/wukong_4of5',
        '/home/zyh/AnyText2/dataset/wukong_5of5',
    ],
    'laion': '/home/zyh/AnyText2/dataset/laion',
    'ocr': '/home/zyh/AnyText2/dataset/ocr_data',
}
```

### 训练命令示例

```bash
# 激活环境
conda activate anytext2

# 完整训练 (使用所有数据)
python train.py

# 使用 200K 子集训练 (测试用)
# 可能需要修改 train.py 中的数据集配置
```

---

## 数据集统计

### AnyWord-3M 规模
- **总图像**: 3,000,000+
- **文本行数**: 9,000,000+
- **语言分布**:
  - 中文: ~1.6M 图像 (~53%)
  - 英文: ~1.39M 图像 (~46%)
  - 其他: ~10K 图像 (~1% - 日韩阿孟印)

### 数据来源
1. **Noah-Wukong** - 中文互联网图像
2. **LAION-400M** - 多语言图文对
3. **专业OCR数据集**:
   - ArT - Arabic 文本
   - COCO-Text - 多语言场景文本
   - RCTW - 中文场景文本
   - LSVT - 中文场景文本
   - MLT - 多语言文本
   - MTWI - 网络图像文本
   - ReCTS - 中文场景文本

---

## 磁盘空间管理

### 当前占用
- ModelScope 缓存: `/home/zyh/.cache/modelscope/hub/datasets/iic/AnyWord-3M` (200 GB)
- 项目副本: `./dataset/` (200 GB)
- **总计**: ~400 GB

### 释放空间建议

```bash
# 1. 验证项目副本完整性
du -sh ./dataset  # 应该显示 200G
ls -lh ./dataset/laion/*.zip  # 检查文件存在

# 2. 确认无误后删除缓存
rm -rf /home/zyh/.cache/modelscope/hub/datasets/iic/AnyWord-3M

# 3. 清理 ModelScope 其他缓存 (可选)
modelscope clear-cache

# 4. 最终验证
df -h  # 查看可用磁盘空间
```

---

## 快速命令参考

```bash
# 查看数据集大小
du -sh ./dataset

# 查看 LAION 内容
ls -lh ./dataset/laion/

# 查看悟空数据
du -sh ./dataset/wukong_*/

# 查看 OCR 数据
ls -lh ./dataset/ocr_data/

# 解压 JSON 配置
cd ./dataset && unzip anytext2_json_files.zip

# 开始训练
cd /home/zyh/AnyText2
conda activate anytext2
python train.py
```

---

## 故障排查

### Q: train.py 找不到数据集?
**A**: 检查并修改 train.py 中的数据集路径，确保指向 `./dataset/`

### Q: 磁盘空间不足?
**A**: 删除 ModelScope 缓存: `rm -rf /home/zyh/.cache/modelscope/hub/datasets/iic/AnyWord-3M`

### Q: ZIP 文件损坏?
**A**: 重新下载或使用缓存副本: `cp -r /home/zyh/.cache/modelscope/hub/datasets/iic/AnyWord-3M ./dataset/`

### Q: 需要解压所有数据吗?
**A**: 不一定。某些训练脚本可以直接读取 ZIP 文件。具体看您的使用场景。

---

## 成功下载的方法总结

✅ **最终成功的方法**:
```bash
modelscope download --dataset iic/AnyWord-3M
cp -r /home/zyh/.cache/modelscope/hub/datasets/iic/AnyWord-3M ./dataset/
```

✅ **为什么成功**:
- ModelScope CLI 1.33.0 稳定可靠
- 不需要处理 Python SDK 的依赖问题
- 自动下载所有 LFS 大文件
- 一次性完成，无需额外步骤

✅ **其他尝试过的方法**:
- Git LFS: 99% 完成，但需要 `git lfs pull`
- Python SDK: 遇到依赖冲突和网络问题
- 手动下载: 文件太多，不现实

---

## 现在可以开始了！

数据集已经就绪，您可以：

1. ✅ **验证数据集** - 检查文件完整性
2. ✅ **开始训练** - 修改路径后运行 `python train.py`
3. ✅ **评估模型** - 使用 `eval/` 目录下的脚本
4. ✅ **释放空间** - 删除缓存节省 200 GB

**祝训练顺利！** 🚀

---

*创建时间: 2026-01-05*
*数据大小: 200 GB*
*状态: ✅ 完整就绪*
