# 如何使用原始 AnyText2 模型做实验

## 📝 说明

本文档教会你如何不使用自定义的 student_model，而是使用原始的 AnyText2 模型进行实验。

---

## 🎯 核心思路

原始 AnyText2 模型的使用非常简单，主要有三个文件：

1. **`ms_wrapper.py`** - 模型包装器，提供高级 API
2. **`demo.py`** - Gradio Web 界面
3. **`simple_demo.py`** - 简单推理示例

你只需要：
1. 加载模型
2. 准备输入数据
3. 调用 `model.forward()`
4. 保存结果

---

## 📦 我为你创建的文件

### 1. 完整使用指南
**文件**: `ANYTEXT2_USAGE_GUIDE.md`

包含：
- 环境准备
- 模型下载与准备（两种方案）
- 四种推理使用方法
- 训练流程
- 实验建议
- 常见问题解答

### 2. 快速开始指南  
**文件**: `QUICK_START.md`

5分钟快速上手：
- 3步安装流程
- 三种使用方式对比
- 快速实验示例
- 参数说明表

### 3. 实验脚本
**文件**: `experiment_anytext2.py`

特点：
- 顶部配置区域，修改参数即可实验
- 支持单生成和批量实验两种模式
- 自动保存结果和调试信息

### 4. 自定义实验模板
**文件**: `custom_experiment_template.py`

特点：
- 4个预设实验示例
- 可直接运行或修改
- 展示不同实验场景

---

## 🚀 最快上手流程

### 步骤1: 安装环境

```bash
conda env create -f environment.yaml
conda activate anytext2
```

### 步骤2: 下载模型

```bash
python -c "from modelscope import snapshot_download; snapshot_download('iic/cv_anytext2')"
```

这会下载官方预训练模型到 `./models/iic/cv_anytext2`

### 步骤3: 运行Demo

```bash
python demo.py
```

然后在浏览器打开显示的URL（通常是 http://localhost:7860）

---

## 💡 使用实验脚本

### 方法一: 简单测试

```bash
# 已有的简单推理脚本
python simple_demo.py
```

### 方法二: 配置化实验

1. 编辑 `experiment_anytext2.py` 顶部配置：

```python
# 在这里修改你的实验参数
EXPERIMENT_TYPE = "text_generation"
IMG_PROMPT = 'A coffee cup with text'
TEXT_PROMPT = 'that reads "Coffee Time"'
SEED = 42
IMAGE_COUNT = 4
# ... 等等
```

2. 运行实验：

```bash
python experiment_anytext2.py
```

3. 查看结果：

```bash
ls experiment_results/
# 会看到生成的图片和调试信息
```

### 方法三: 批量对比实验

编辑 `experiment_anytext2.py`:

```python
EXPERIMENT_TYPE = "batch"  # 改为批量模式
```

运行后会测试不同控制强度 (0.5, 0.75, 1.0, 1.25, 1.5) 的效果对比。

### 方法四: 自定义实验

使用模板文件：

```bash
# 复制模板
cp custom_experiment_template.py my_experiment.py

# 编辑模板，修改实验函数
vim my_experiment.py

# 运行你的实验
python my_experiment.py
```

---

## 🔑 核心API说明

### 加载模型

```python
from ms_wrapper import AnyText2Model

model = AnyText2Model(
    model_dir='./models/iic/cv_anytext2',  # 模型目录
    use_fp16=True,                         # 使用半精度（省显存）
    use_translator=False,                  # 是否用翻译器（支持中文）
    font_path='font/Arial_Unicode.ttf'     # 字体路径
).cuda(0)
```

### 推理调用

```python
# 准备输入
input_data = {
    'img_prompt': '图像描述',
    'text_prompt': '文字内容（用双引号包裹）',
    'seed': 42,
    'draw_pos': 位置图或None,
    'ori_image': 原图（编辑模式需要）或None
}

# 准备参数
params = {
    'mode': 'text-generation',  # 或 'text-editing'
    'image_count': 4,
    'ddim_steps': 20,
    'image_width': 512,
    'image_height': 512,
    'strength': 1.0,
    'cfg_scale': 9.0,
    # ... 更多参数
}

# 执行推理
results, code, warning, debug = model.forward(input_data, **params)

# 保存结果
if results:
    for i, img in enumerate(results):
        Image.fromarray(img).save(f'output_{i}.png')
```

---

## 🎨 常见实验场景

### 实验1: 测试不同提示词

```python
# 修改这些组合来测试效果
IMG_PROMPT = 'A street sign'
TEXT_PROMPT = 'that reads "Main Street"'

IMG_PROMPT = 'A book cover'
TEXT_PROMPT = 'with title "My Book"'

IMG_PROMPT = 'A restaurant menu'
TEXT_PROMPT = 'that says "Menu"'
```

### 实验2: 测试不同参数

```python
# 控制强度
STRENGTH = 0.5   # 弱控制
STRENGTH = 1.0   # 标准
STRENGTH = 1.5   # 强控制

# CFG强度
CFG_SCALE = 7.0   # 更自由
CFG_SCALE = 9.0   # 标准
CFG_SCALE = 12.0  # 更严格

# 采样步数
DDIM_STEPS = 10   # 快速但质量较低
DDIM_STEPS = 20   # 标准
DDIM_STEPS = 30   # 更高质量
```

### 实验3: 字体和颜色控制

```python
# 使用不同字体
FONTS = [
    'IndieFlower',           # 英文手写体
    '站酷快乐体2016修订版',   # 中文
    '阿里妈妈东方大楷',       # 中文
    'No Font(不指定字体)',    # 随机
]

# 指定颜色
COLORS = [
    '255,0,0',     # 红色
    '0,255,0',     # 绿色  
    '0,0,255',     # 蓝色
    '500,500,500', # 随机
]
```

---

## 📊 理解输出

运行后会得到：

```
results = [
    生成的图像1,      # numpy数组 (H, W, 3)
    生成的图像2,
    生成的图像3,
    生成的图像4,
    debug_glyph,      # 调试: glyph位置图 (如果show_debug=True)
    debug_font_hint   # 调试: 字体风格图 (如果show_debug=True)
]
```

---

## ❓ 常见问题

### Q: 如何生成中文？

**方法1** (推荐): 使用翻译器
```python
model = AnyText2Model(
    model_dir='./models/iic/cv_anytext2',
    use_translator=True,  # 启用翻译
)

TEXT_PROMPT = '上面写着"你好" "世界"'
```

**方法2**: 直接输入
```python
# 即使不启用翻译，直接输入也可以（但效果可能稍差）
TEXT_PROMPT = 'that reads "你好" "世界"'
```

### Q: 显存不够怎么办？

```python
# 1. 使用fp16半精度
use_fp16=True

# 2. 禁用翻译器（节省~4GB）
use_translator=False

# 3. 减小图片尺寸
image_width = 384
image_height = 384

# 4. 减少生成数量
image_count = 1
```

### Q: 如何指定文字位置？

创建位置图：
```python
import cv2
import numpy as np

pos_img = np.zeros((512, 512, 1), dtype=np.uint8)
# 绘制白色区域表示文字位置
cv2.rectangle(pos_img, (50, 100), (250, 180), 255, -1)

input_data['draw_pos'] = pos_img
```

### Q: 如何使用自己的模型或LoRA？

```python
params = {
    'base_model_path': '/path/to/your_model.ckpt',
    'lora_path_ratio': '/path/to/lora1.pth 0.7 /path/to/lora2.pth 0.3',
    # ...
}
```

---

## 🎓 进阶: 训练自己的模型

如果你想从头训练:

### 1. 准备基础模型

```bash
# 下载SD1.5模型（需要从HuggingFace等来源获取）
# 假设路径为: /path/to/v1-5-pruned.ckpt

# 创建AnyText2训练用检查点
python tool_add_anytext.py /path/to/v1-5-pruned.ckpt ./models/anytext2_sd15_scratch.ckpt
```

### 2. 准备数据集

参考 `download_dataset.py` 和 `t3_dataset.py` 准备训练数据。

### 3. 开始训练

```bash
# 编辑 train.py 设置数据集路径
# 然后运行:
python train.py
```

---

## 📚 文档索引

- **`QUICK_START.md`** - 5分钟快速上手（推荐先看这个）
- **`ANYTEXT2_USAGE_GUIDE.md`** - 完整使用指南（详细参考）
- **`CLAUDE.md`** - 项目技术文档（架构和原理）
- **`experiment_anytext2.py`** - 配置化实验脚本
- **`custom_experiment_template.py`** - 自定义实验模板

---

## ✅ 快速检查清单

在开始实验前，确保：

- [ ] 已安装conda环境 (`conda activate anytext2`)
- [ ] 已下载模型 (`models/iic/cv_anytext2` 目录存在)
- [ ] 能成功运行 `python demo.py`
- [ ] 理解基本的参数含义

---

## 🎉 开始实验！

现在你可以：

1. **交互式使用**: `python demo.py` → 浏览器操作
2. **快速测试**: `python simple_demo.py` → 看效果
3. **配置实验**: 编辑 `experiment_anytext2.py` → `python experiment_anytext2.py`
4. **自定义实验**: 复制 `custom_experiment_template.py` → 修改 → 运行

祝你的实验顺利！有任何问题可以随时问。 😊
